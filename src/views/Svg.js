import React from 'react'

const Svg = ({ src, alt, width, height }) => {
  return (
    <svg
      xmlns="http://www.w3.org/1999/xlink"
    //   width={width}
    //   height={height}
    //   viewBox={`0 0 ${width} ${height}`}
    >
      {/* <image href={src} alt={alt} width={width} height={height} /> */}
    </svg>
  );
};
export default Svg
